/**
 * NotificationTestSimulator — removed.
 *
 * The development-only notification test simulator has been removed.
 * Production compliance notifications are dispatched deterministically via:
 *   python manage.py process_deadline_notifications
 *
 * This stub exists only to satisfy any stale TypeScript module references
 * during the build cache flush cycle.
 */

export {};
